# v3.2.0 Inventory Search research and implementation plan

## Decision

Better Inventory can provide a standalone inventory search feature without depending on Stuff Searcher. The implementation should be original and use current Darktide filtering contracts rather than porting Stuff Searcher's layout mutation.

The first v3.2.0 implementation should be read-only: it may rank, dim, or hide presented entries, but it must not favorite, equip, purchase, discard, sacrifice, or modify an item. Search must compose with native slot filters and the active native or ItemSorting comparator.

The default unmatched-card behavior is **Dim and promote matches**. Every entry remains available, matching entries form the highest-priority sort group, and unmatched cards receive a lightweight widget-level fade. A dropdown also offers **Hide unmatched**, which uses the native filter path. An empty query applies neither behavior and must be presentation-identical to v3.1.0.

## Research inputs

- Research archive: `stuff_searcher-47-1-2-1680938124.zip`
- Archive SHA-256: `41EFBF52035549B384EE3212919DDCAAD05DEB37D7BABB4BD2550E7A7A37F11E`
- Archived release: Stuff Searcher 1.2, last updated in April 2023.
- Current source contracts were checked under `Darktide-Source-Code/scripts/ui/views`.
- Nexus description and historical roadmap: <https://www.nexusmods.com/warhammer40kdarktide/mods/47>
- Nexus bug list: <https://www.nexusmods.com/warhammer40kdarktide/mods/47?tab=bugs>

No Stuff Searcher runtime source should be copied into Better Inventory. Its behavior and public roadmap are useful requirements, and its Nexus permissions permit credited fixes or improvements, but an independent design gives Better Inventory clearer ownership and avoids carrying obsolete assumptions.

### Historical request and defect triage

The visible Stuff Searcher forum and bug titles are requirements evidence, not code to port:

| Historical item | v3.2.0 disposition |
| --- | --- |
| Added `&` operator | Actionable. Implement bounded, quote-aware explicit AND in the original parser. |
| Unfocus when selecting item | Actionable. Selecting a card defocuses the text field without clearing the query, then the native select/inspect action proceeds. |
| Stuff Searcher for Penances? | Out of scope. Penances are not inventory entries and do not share the item-grid projection contract. |
| Suggestion for alignments / Question | Do not infer a setting from a title alone. Reassess only when the post body is available; the initial UI remains responsive and view-owned. |
| Search bars missing after Unlocked and Loaded / filtering no longer functions | Architecture requirement. Detect each current view contract, never depend on removed private fields, and fail open when an adapter or scenegraph node is unavailable. |
| `localize_en` nil | Architecture requirement. Use current-locale localization plus bounded built-in English aliases; never construct a second `LocalizationManager` or depend on a block-local helper. |
| `input_field == nil` | Lifecycle requirement. All input access is guarded and view-local; missing/destroyed UI disables that view's search UI without crashing or hiding items. |
| Errors with Inventory2D | Compatibility requirement. Do not mutate shared definitions or another mod's layout snapshot; compose through live native filtering and the existing comparator manager. |
| Random crashes on loadout changes | Lifecycle requirement. Selection/loadout changes may invalidate projections, but closed-view state, widgets, callbacks, and layout copies must be released and stale callbacks rejected. |
| Does this mod still work? / Many errors / Update | Evidence that a current-contract rewrite and source-contract tests are required; these are not standalone features. |

## Why Stuff Searcher stopped working

Stuff Searcher 1.2 does not have a reusable query engine. `search.lua` contains empty function stubs; the behavior is implemented directly in `stuff_searcher.lua` as view mutation and input handling.

The principal break is structural:

1. It copies `view._filtered_offer_items_layout` once into `view._stored_layout`.
2. It filters that snapshot into another table.
3. It writes the result back to `view._filtered_offer_items_layout`.
4. It asks the view to sort and present the replaced table.

Current Darktide no longer owns `_filtered_offer_items_layout`. `ItemGridViewBase._present_layout_by_slot_filter` now reads the live `view._offer_items_layout`, applies native slot/type filtering plus `_filter_by_filter_option`, and passes the resulting table to `_sort_grid_layout`. Replacing the removed field therefore has no effect.

Even before that field was removed, the design had additional correctness and compatibility problems:

- The one-time `_stored_layout` becomes stale after a purchase, discard, item replacement, store rotation, asynchronous inventory refresh, or tab change.
- The search input and active view are stored globally on the mod, allowing one view's state to leak into another view or a hot reload.
- Search strings are rebuilt with repeated localization and MasterItems lookups for every item on every changed query.
- Only `%` is escaped before `string.find` uses Lua-pattern mode. `.`, `+`, `-`, `*`, `?`, `[`, `]`, `(`, `)`, `^`, and `$` remain pattern syntax and can mis-match or error.
- The non-English helper is declared local inside an initialization block, then referenced outside that scope. This produces the reported `localize_en` nil error.
- It temporarily hooks `LocalizationManager.init` and manually constructs a second manager, which is unsafe global lifecycle ownership for a search feature.
- It scans raw keyboard keystrokes every frame and hard-codes `S`, rather than using a configurable input action and view-local focus ownership.
- It globally changes `ViewElementGrid`, `ViewElementTabMenu`, and `ViewElementInputLegend` behavior while one global textbox is active.
- Empty results require unrelated preview/equip guards because the filter is applied outside the native selection lifecycle.
- Curio and perk text uses localized descriptions as an unstructured pattern corpus. Percent values therefore collide with Lua pattern syntax and numeric searches are substring-based.
- The community `&` patch is not present in the 1.2 archive and splits strings with another Lua pattern. It supplies useful behavior, but not a bounded parser.

These defects also explain the historical Stuff Searcher conflicts with Inventory2D and ItemSorting. The feature changed the layout and then separately called sorting, so whichever mod owned the last private-field or comparator assumption won.

## Current Darktide seams

### Standard inventory and vendor views

`ItemGridViewBase._present_layout_by_slot_filter` is the authoritative composition point for:

- `InventoryWeaponsView`;
- `CreditsVendorView` and its Armoury/GlobalStore routes;
- `MarksVendorView`;
- `CraftingMechanicusModifyView`.

Its order is:

1. read the current `_offer_items_layout`;
2. apply slot and item-type filters;
3. call `_filter_by_filter_option(entry)`;
4. pass the filtered copy to `_sort_grid_layout`;
5. preserve the selected gear ID while re-presenting.

In **Hide unmatched** mode, Better Inventory should chain `_filter_by_filter_option`:

```lua
return original_native_filter(view, entry) and Search.matches(view, entry)
```

In **Dim and promote matches** mode, `_filter_by_filter_option` returns the original native result without removing unmatched entries. The precomputed match bit becomes the outermost Better Inventory comparator key instead. The comparator must only read cached booleans; it must never localize, project, parse, or allocate while `table.sort` is running.

The wrapper around `_present_layout_by_slot_filter` records the latest slot filter, item-type filter, and display name in view-local state. Hide mode may replay that native method because membership changes. Dim/promote mode must not replay it: native presentation creates fresh unkeyed spacer entries and therefore regenerates every card. It instead sorts the canonical, already natively eligible grid entries into the inactive one of two bounded view-owned buffers and calls `update_grid_layout`; `_offer_items_layout` and the grid's canonical `_grid_layout` remain untouched.

This ordering also gives ItemSorting its expected `optional_filter_layout` in hide mode. In dim mode, the indexed current ItemSorting/native comparator orders the canonical eligible entries inside the matched and unmatched groups. Better Inventory adds the search key to its existing comparator wrapper instead of wrapping the same comparator a second time.

### Hadron sacrifice view

`CraftingMechanicusBarterItemsView` owns a separate `_item_grid_layout` and `_sort_grid_layout` implementation. It needs a small explicit adapter rather than pretending it follows `ItemGridViewBase`'s optional-filter contract.

The adapter must:

- keep the authoritative `_item_grid_layout` untouched;
- build a filtered presentation copy from that live table only for hide mode;
- preserve external spacing entries in their native positions;
- use the same identity-preserving dim/promote grid lane when the canonical grid contract is available, otherwise fail open to its existing presentation callback;
- rebuild after sacrifice-state, mastery-pattern, selection, and inventory changes;
- release the copy and search index when the view closes.

This adapter should land only after its lifecycle tests pass. It must not temporarily replace `_item_grid_layout` around a native call because an error or asynchronous callback could strand the filtered copy as the source of truth.

## Proposed v3.2.0 feature surface

### Simple search

Plain text searches a normalized literal corpus. Search is case-insensitive through `Utf8.lower`, and `string.find(..., 1, true)` must always use literal mode.

The corpus should include, when available:

- Better Inventory custom name and imported Name It name;
- localized weapon family, pattern, and mark;
- localized Curio/item name;
- blessing names;
- perk names, canonical Darktide descriptions, and both Better Inventory compact-description forms;
- localized effective rarity and item type;
- stable master-item, trait, localization, and mark identifiers as fallback aliases;
- visible total and base ratings as discrete numeric fields.

Do not call a hookable full weapon-name/stat projection for every item. In particular, indexing must not invoke GodRolls' decorated-name calculation. Resolve native localization IDs directly, then add Better Inventory's stored custom name explicitly.

### Effective rarity and native rarity

Search must treat Better Inventory's `Sainted` presentation tier as distinct from Darktide's stored `Transcendent` rarity.

Darktide stores ordinary quality in `item.rarity`: `1` Profane, `2` Redeemed, `3` Anointed, `4` Exalted, and `5` Transcendent. Better Inventory's Custom legendary tier does not mutate that value. When the feature is active and `CustomTier.matches(item)` accepts a rarity-5 weapon or Curio, `Items.rarity_display_name` instead presents native localization tier `6`, currently `Sainted` in English.

The search projection therefore carries two fields:

- **effective rarity**: what the player currently sees. A qualifying custom-tier item is `Sainted`; a non-qualifying rarity-5 item is `Transcendent`;
- **native rarity**: the underlying Darktide value and label. Both of those items remain native `Transcendent`/`5`.

Plain rarity words and the `rarity:`/`quality:` fields use **effective rarity**. Consequently:

```text
sainted                 -- only currently qualifying Custom legendary tier items
transcendent            -- only visible Transcendent items, excluding Sainted items
rarity:sainted          -- same explicit effective-tier filter
rarity:transcendent     -- same explicit effective-tier filter
native-rarity:transcendent -- every stored rarity-5 item, including Sainted items
```

An exact bare clause equal to a known localized rarity name or built-in English rarity alias compiles as a rarity predicate, so `Sainted`, `Anointed`, or `Redeemed` means “only this effective rarity,” not “this text appears somewhere.” A custom item name containing one of those words remains searchable with `name:sainted`. Partial text such as `saint` may still use the normal literal corpus.

Current-locale rarity labels come from `RaritySettings` localization keys. English aliases are a small static table for cross-locale usability; search must not recreate the removed Stuff Searcher `localize_en` lifecycle. Numeric native values are accepted only by `native-rarity:` to avoid conflating visible and stored tiers.

The effective tier must be derived from the same active `CustomTier.matches` provider used by presentation, independent of which mod owns background colors. Disabling Custom legendary tier makes `Sainted` match nothing and returns all stored rarity-5 items to effective `Transcendent`. Any custom-tier enable/criteria change invalidates the affected search projections and requests one coalesced refresh.

### Explicit AND

`&` separates required clauses outside quotes:

```text
power & cycler
dueling & riposte & uncanny
sword & uncanny & flak
```

The empty-field example is context-specific: melee/ranged inventory keeps `sword & uncanny & flak`, while Curio inventory uses `toughness & health & revive` to demonstrate primary and secondary line ranking.

Plain terms search every indexed equipment field, so users do not need to type
`blessing:` or `perk:` for ordinary searches. Whitespace around `&` is optional.
Empty clauses are ignored. A quoted `&` remains literal.

### Field-qualified clauses

Initial aliases:

| Canonical field | Accepted examples |
| --- | --- |
| `name` | `name:plasma`, `name:"dueling sword"` |
| `blessing` | `blessing:uncanny`, `bless:cycler` |
| `perk` | `perk:carapace`, `perk:flak`, `perk:"flak dmg"` |
| `type` | `type:melee`, `type:curio` |
| `mark` | `mark:iv` |
| `rarity` | `rarity:sainted`, `rarity:transcendent`, `quality:anointed` (effective visible tier) |
| `native-rarity` | `native-rarity:transcendent`, `native-rarity:5` (underlying Darktide tier) |
| `rating` | `rating:500`, `rating:>=490`, `rating:480..500` |
| `base` | `base:380`, `base:>=370` |
| `favorite` | `favorite:true` |
| `equipped` | `equipped:true` |
| `new` | `new:true` |

Unknown field names should be treated as literal text in simple mode, not as a fatal parse error. OR, nested groups, arbitrary Lua patterns, user scripts, and separate quick-filter controls are out of scope. State and type conditions remain available through the text fields above without duplicating Better Inventory's existing inventory controls.

### Unmatched-card behavior and sort hierarchy

The unmatched-card behavior is selected in Better Inventory's Mod Options:

| Value | Behavior |
| --- | --- |
| `Dim and promote matches` | Default. Matching cards move above every unmatched card. Unmatched cards remain interactive and receive a uniform fade. |
| `Hide unmatched` | Non-matching entries are removed through Darktide's native filter composition. |

Within the currently selected native tab/type, ordering is strictly:

1. search relevance group, when a non-empty query is active;
2. for Curios, total matching primary/secondary line occurrences, then the number of distinct query clauses covered, then primary-line clause coverage from query-left to query-right, then overall clause coverage from left to right;
3. for weapons, distinct query clauses covered and then clause coverage from query-left to query-right;
4. inside each search-relevance tier, equipped before unequipped when equipped/favorite priority is enabled, then visible item level descending;
5. Better Inventory equipped/favorite priority for ordinary matches and exact ties;
6. Better Inventory perfect-roll priority;
7. the selected ItemSorting or native Darktide comparator.

Relevance is stronger than equipped state. For `toughness & ability & revive`, a Curio with Toughness on both its primary and secondary lines plus Ability Regeneration and Revive Speed has four line hits and ranks first. Three distinct matching lines rank next, with a matching primary line ahead of three secondary lines. Two-line and one-line partial matches then follow; equal hit/coverage groups prefer earlier query clauses, so Toughness+Ability precedes Toughness+Revive, which precedes Ability+Revive. Within one exact relevance tier an equipped item level 410 Curio precedes an unequipped item level 430 Curio; when equipped state is equal, 430 precedes 420, then 410. Partial literal clauses use the same hierarchy, so `heal` ranks Health primary and secondary lines exactly as `health` does. A Curio matched only by its name or another property remains in the ordinary matched group. Every entry without any relevant clause retains the complete pre-search hierarchy.

In default Dim and promote mode, incomplete AND matches remain dimmed but are ordered by this partial relevance instead of collapsing into one undifferentiated group. Hide unmatched retains strict AND semantics and removes incomplete matches. This keeps the dropdown's meaning intact while making the default exploratory mode progressively useful.

Search never overrides native tab, slot, item-type, ownership, or store eligibility. A matching ranged weapon cannot appear in a melee or Curio tab.

The search rank must be a separate comparator decision, not an offset added to the existing Better Inventory priority. Its bounded Curio relevance/equipped/item-level tuple is packed once during the projection scan and read in O(1) by the comparator. The selected comparator executes only for exact tuple ties. When the query is empty, all entries have the same search rank and the wrapper is behavior-neutral.

Use `widget.alpha_multiplier` for the default fade. Darktide's `UIWidget.draw` already multiplies this scalar across the entire card, including its background, image, icons, and text. This preserves custom colors and avoids cloning or rewriting every pass style. A value near `0.40` is the initial live-QA target. `color_intensity_multiplier` may be tested as a small companion adjustment, but true grayscale is out of scope because it would require per-pass color ownership or a custom material/shader.

Dimmed cards remain clickable, focusable, and inspectable. The right detail panel remains at full opacity. Hide mode changes only list membership, not any account or item state.

### Search UI

The in-view UI should contain only one compact text field. Supporting behavior remains in Mod Options:

- the unmatched-card behavior setting;
- a **Remember searches during this game session** option, defaulting to Off;
- a configurable focus input action, defaulting to Off until a conflict-free action is selected;
- Escape to defocus the field first, then allow the normal view exit on the next press;
- controller Up from any first-row item moves focus into the field, Confirm invokes Darktide's native text-entry path (including Xbox/PlayStation virtual keyboards), and Down returns to the current result grid's first item without suppressing unrelated grid, tab, purchase, equip, compare, or legend input.

The widget must be created from Better Inventory's per-view cloned definitions or a Better Inventory-owned view element. It must not mutate the shared module definition table returned by `hook_require`.

When a result disappears, native gear-ID selection restoration should choose the same matching item when possible or the first valid result. Hide mode may present an empty native grid, without patching preview or equip functions.

### Query lifetime and optional session memory

By default, query text remains active while the current view is alive, including item selection, tab changes, sorting changes, native re-presentation, store refreshes, and crafting replacement. Closing that view clears the query. This is the least surprising default for **Hide unmatched**, because a later reopen cannot appear empty due to a forgotten filter.

When **Remember searches during this game session** is enabled, Better Inventory retains only the bounded query string after a view closes. The record is keyed by the active character and logical view family so an Inventory query is not silently applied to Armoury/Requisition, Hadron, or Melk. Reopening the same family for the same character restores the text, compiles a fresh query, and matches the current authoritative layout.

Session memory never stores a compiled query, closure, widget, view, layout, item table, match result, or projection cache. Clearing the text replaces the remembered record with an empty query. Character change, mod disable, Ctrl+Shift+R hot reload, and game exit clear all remembered searches. The existing unmatched-card behavior dropdown remains an ordinary mod setting and is not duplicated into session memory.

## Proposed module boundaries

| Module | Responsibility |
| --- | --- |
| `BetterInventory_search_query.lua` | bounded tokenizer/parser, literal normalization, numeric comparator compilation |
| `BetterInventory_search_index.lua` | fail-soft extraction of searchable item fields and bounded per-item cache |
| `BetterInventory_search_runtime.lua` | view-local state, native-filter composition, refresh scheduling, and commit routing |
| `BetterInventory_search_integration.lua` | supported-view adapters and identity-preserving dim/promote grid reorder |
| `BetterInventory_search_ui.lua` | cloned definitions, compact text-field geometry, focus, and input ownership |
| `BetterInventory_search_hooks.lua` | isolated native-grid, input, focus, and Hadron sacrifice hook ownership |

`BetterInventory_features.lua` exposes only the composition facade. Search-only hooks live in `BetterInventory_search_hooks.lua`; this preserves the repository's single-hook-owner rule and keeps `BetterInventory_runtime.lua` below its 100 KB architecture ceiling.

## Implementation status

The v3.2.0 branch now implements the complete first-release scope:

- bounded literal parser, quoted `&`, typed fields, numeric comparisons, and fail-open invalid input;
- weak, fingerprinted projections for current-locale native/custom names, family/mark, perks, blessings, ratings, state flags, and split effective/native rarity, including Simplified Chinese UTF-8 names, concise visible Curio line aliases, explicitly scoped trait descriptions, and rarity labels;
- search-first stable ranking with cached Curio primary/secondary relevance over the existing equipped/favorite, perfect-roll, and ItemSorting/native hierarchy;
- default dim-and-promote behavior, optional hide behavior, and one compact query field placed with independently configurable Inventory/Armoury top and bottom padding below native headers; defaults are Inventory `14 / 46` and Armoury `22 / 34` (top / bottom) without changing native titles;
- a search-owned top mask inset that clips scrolled cards below the field while preserving each grid's native bottom edge, plus a CreditsVendorView post-update flush shared by Armoury Requisition and GlobalStore;
- per-character/view-family session memory (default Off), optional keyboard focus action, first-row controller focus transfer, Escape-first defocus, and card-selection defocus without query clearing;
- current-contract adapters for Inventory, Hadron modify, Armoury/Requisition, GlobalStore/general vendors, Melk, and the separate Hadron sacrifice grid;
- per-view cloned definitions, missing-geometry fail-open behavior, 80 ms coalescing, one reusable weak relevance-rank map, alpha ownership restoration, and explicit close/hot-reload release;
- direct custom-name invalidation, dynamic favorite/equipped/new/loadout fingerprints, GodRolls/God Stat Checker presentation isolation, and one bounded Stuff Searcher compatibility warning.
- production master-item perk paths resolve through their gameplay trait IDs before compact-label lookup; every trait/perk identity is projected first, compact perk aliases next, and optional long descriptions last, so Enhanced Descriptions cannot starve `flak`, `unyielding`, or later perk slots from the bounded record.
- the finalized performance path warms cold rich projections in bounded 16-item slices, defers parsing and matching until 80 ms of quiet, then trusts those validated records for one scan; it reuses one scan context, stores the bare-text corpus as one bounded string, coalesces one commit, skips repeated input, encodes match and Curio relevance in one rank map, stores dim ownership as scalar alpha values, and bypasses search calls inside native filtering/comparison while no settled query is active.
- after the quiet interval, default dim/promote mode resolves the selected comparator by Darktide's current index and commits one synchronous `ViewElementGrid.update_grid_layout` reorder over the existing card identities. Two bounded view-owned layout buffers prevent clearing the live layout while the next order is built; canonical external spacing and selected-card identity are preserved. Hide mode retains the native filter/presentation transaction, and returning from hide performs one full membership restoration before re-entering the in-place lane. Unsupported or drifted grids fail open to native presentation.
- controller search focus uses Darktide's native per-grid input-disabled state on the supported view's main grid. Cursor-mode typing deliberately leaves that grid live because `ViewElementGrid` itself owns wheel scrolling, hover, and card presses; its card callback defocuses search without clearing the query. Both modes retain transition-only input-legend ownership so Back defocuses before closing. There are no global `ViewElementGrid.update` or `ItemGridViewBase.update` search wrappers; Inventory reuses Better Inventory's existing view update/input hooks, grids outside supported views pay no grid-update callback, and a grid already disabled by native discard/options flow retains that ownership after defocus.

### Final performance audit

`tools/benchmark_search.py` now uses Darktide-shaped `{ item = item }` entries and 128 realistic weapon projections containing long Enhanced Descriptions blessing text plus two searchable perks. Ten final dim/hide runs report all 128 records built once in eight bounded slices, all 128 reused by the settled query, `0-1 ms` total for a synthetic burst of 400 text changes, `0-1 ms` for the one settled match pass, and about `8.55 KiB` transient allocation for that pass. Windows `os.clock` has millisecond granularity, so these absolute sub-millisecond figures are regression indicators rather than engine-frame promises.

The final 128-item projection state is approximately `415.7 KiB`; explicit release and full collection reclaim approximately `413.8 KiB`, with no positive retained delta. Six thousand direct settled-runtime calls take `0-1 ms` in aggregate (`0-0.167 microseconds` reported average); the production view-local gate takes `0-1 ms` for the same 6,000 checks and invokes the runtime zero times. Cache warming totals `13-15 ms` in this synthetic runtime but is divided across eight frames; no frame processes more than sixteen rich projections. Real input-string allocation belongs to Darktide's text widget and is intentionally excluded from the 400-change runtime transaction measurement.

`tools/benchmark_search_ui.py` measures the remaining production settled path: one bounded field revision poll plus mouse-mode view input handling. Runs of 10,000, 100,000, and 1,000,000 frames report `0.348-0.400 microseconds` per combined frame. Transient state remains the same `0.312 KiB` at all three lengths rather than scaling with frame count. These Lupa measurements are regression evidence, not substitutes for Mod Performance Monitor's in-engine high-resolution timer.

`tools/benchmark_search_grid.py` exercises the production in-place adapter with 128 Darktide-shaped card identities, native top/bottom anchors, selection restoration, and changing search ranks. One thousand completed reorders average about `71 microseconds`; all widget identities survive, retained state is about `2.4 KiB`, and the approximately `3.5 KiB` transient work per completed query comes from Darktide's two `update_grid_layout` reorder arrays. It is query-time, not frame-time, and fully collects. The adapter retains exactly two layout buffers rather than retaining one layout generation per query.

Correctness is retained by separating cache trust from validation: only a query edit may use `trust_cache`; native presentation capture, store/inventory composition, refresh, and explicit invalidation still calculate the full item/customization/state fingerprint. Custom-name, tier, favorite/equipped/loadout, crafting, purchase, discard, and view lifecycle callbacks retain their existing invalidation or presentation paths. The runtime still fails open on projection failure and keeps all caches weak and view-owned.

The automated release gate passes all 51 behavior scripts, including a production-module composition test that promotes a low-native-priority weapon independently by `shovel`, `flak`, and `uncanny`, then verifies cache reuse and release. Search core modules meet their configured risk-weighted Lua coverage thresholds. Version metadata, generated manifests, and release packaging are complete. Remaining release work is live visual/gamepad and engine-profiler QA.

## Live optimization regression investigation

This journal is authoritative for trial-and-error work after the first search implementation. A synthetic pass is not considered a fix until the same build promotes and dims live Darktide cards while Mod Performance Monitor remains within the settled-frame budget.

| Revision / attempt | Hypothesis or change | Live result | Status |
|---|---|---|---|
| Pre-performance baseline through `6e5a0fa` | Direct query matching and presentation path; later fixes extended perk indexing and controller focus. | Name/blessing search visibly changed card ordering before the performance pass. Perk projection received additional coverage, but no separate final live confirmation was recorded before optimization began. | Last known functional family; comparison baseline. |
| `23e541c` | Reduce allocation by suppressing duplicate queries, trusting validated projections, and removing immediate resort requests in favor of the scheduled presentation. | CPU concern remained; this revision began moving responsibility away from the immediate query-change path. | Needs contract comparison; not independently live-certified. |
| `b75a331` | Parse and scan only after 80 ms quiet time; warm projections in sixteen-item slices. | Later live build became a complete no-op for `shovel`, `uncanny`, `unyielding`, and Curio `stamina`. | Suspect handoff boundary. |
| `5ac2052` + `8410289` | Remove global `ItemGridViewBase.update` / `ViewElementGrid.update` hooks and gate runtime/filter/comparator work with view scalars. | Performance improved from about `0.190 ms` to about `0.038 ms`, but live queries no longer changed order or alpha. | Performance success, functional regression. |
| `345e72e` | Prefer indexed current sort option over stale ItemSorting pointer; read live widgets through `content.element`; reapply alpha after presentation. | Patch loaded (`_cb_on_present` hook appears in clean log) but live `shovel`, `uncanny`, and `unyielding` remained no-ops. | Hypothesis insufficient; do not treat as resolved. |
| `119d0cf` after `4468e11` diagnostics | Restore one native `_present_layout_by_slot_filter` transaction after the 80 ms quiet interval while retaining all inactive hot-path gates. Darktide's deferred `_sort_grid_layout` also rebuilds widgets, so the removed shortcut had no allocation advantage. | Synced source hashes matched live files, but queries still produced no visible change. The current log contained zero bounded `[SearchTrace]` query records after input. | Presentation repair insufficient; failure is before settlement. |
| `be89f3d` delivery restoration | Restore only the common `ItemGridViewBase.update` text-revision poller used by the last live-functional family; keep the removed `ViewElementGrid.update` card hook absent. | Live trace proved query delivery and matching were restored (`shovel` 2/6, `flak` 4/6), but every settlement had `presentation_kind=nil`; ordering and dimming therefore remained unchanged. | Failed as a complete fix; retained only as the proven low-cost delivery repair. |
| `968370d` presentation-context recovery | When the runtime is recreated under an already-open view, recover the native selected-tab filters/display name (or the external-layout lane) on the next changed query. Retain no layout snapshot; later native presentations still replace this recovered context. | Live inventory promoted matches and dimmed non-matches. Post-reload trace showed `kind=native`, one native presentation, and `result=true` for each settled query. BetterInventory measured about `0.047 ms/frame`, within the `0.050 ms` target. | Live-confirmed fix; temporary trace instrumentation removed afterward. |
| Post-confirmation idle follow-up | A live Curio query (`stamina & curio`) produced the correct relevance hierarchy, but Mod Performance Monitor later showed BetterInventory around `0.12-0.18 ms/frame`; the captured sample was `0.134 +/- 0.119 ms`, low confidence, eight calls, after repeated hot reloads and with Lua at 549 MB. | Static tracing found two search-owned callbacks still running on every settled inventory frame: `ItemGridViewBase.update` field polling and the global input-legend guard. Offline settled UI polling was only `0.376 us/frame` with zero transient allocation, so the displayed total could not be attributed to O(n) matching or a retained scan. | Valid concern; source attribution and callback-count reduction required before release. |
| Current callback-consolidation candidate | Fold field delivery/runtime gating into BetterInventory's existing `InventoryWeaponsView.update` and `CreditsVendorView.update` callbacks; install one explicit update only for other supported views. Replace the global input-legend hook with transition-only action ownership restored at the post-update boundary. | Automated search/controller/lifecycle tests pass. The settled inventory path loses two monitor-visible callbacks; ranking, deferred settlement, same-frame Back protection, native pre-disabled grid ownership, and supported vendor/Hadron coverage remain exercised. | Awaiting live performance confirmation; do not mark final solely from synthetic timing. |
| `a8b977b` live callback consolidation | Same consolidated callback candidate, synced to the installed mod. | Search remained functional. Live sample fell to about `0.061 ms/frame` and four callbacks/frame, but repeated completed searches appeared to raise the displayed mean toward `0.150 ms/frame`. | Functional and materially improved; repeated-query growth required a separate retained-state audit. |
| Repeated-query bounded-state pass | Clear and reuse the one view-owned result map each settled scan; remove the redundant generation map; clear and reuse the sort-priority cache instead of allocating another weak table per presentation. | A 100-query soak deliberately retains all 100 previous native-shaped layouts while asserting the live result map stays at exactly 24 current entries. Synthetic 128-item runs complete 100 settled scans in `18-19 ms` total (`0.18-0.19 ms` runtime average each), with `0.038-3.046 KiB` retained after full collection and zero settled production-gated runtime calls. Targeted runtime and sorting tests pass. | Candidate removes real query-to-query garbage/retention. Four callbacks/frame remain: reducing controller/input and shared lifecycle ownership further is not justified without a safe event seam. Live verification still required. |
| Native full-presentation architecture (failed) | Reuse the normal `_present_layout_by_slot_filter` transaction for each settled dim/promote query. | Live cards briefly disappeared and reappeared on every query; BetterInventory climbed to roughly `0.18-0.20 ms/frame`, while Lua heap reached roughly `370 MB` after repeated searches. Source tracing proved `ItemGridViewBase.present_grid_layout` creates a fresh spacing entry without an `entry_id`; `ViewElementGrid` therefore selects `generate_grid`, destroys all widgets, regenerates content blueprints/cards, and defers the replacement. | Confirmed architectural failure; retained only for hide mode, where membership truly changes. |
| `7bab4d0` identity-preserving dim/promote | Build the search order from the current grid's canonical layout/alignment identities, preserve native external anchors and selection, and swap between two bounded arrays through `update_grid_layout`. Keep native presentation as a fail-open fallback and for hide/post-hide restoration. | Clean-process live confirmation after four searches held BetterInventory at about `0.045 ms/frame` and Lua near `200 MB`; ordering/dimming worked and card flicker was gone. The 128-card/1,000-query synthetic run preserves every widget identity, averages about `71 us` per commit, and retains about `2.4 KiB`. | Live-confirmed performance and memory fix. |
| Cursor focus lock follow-up | The field previously called `ViewElementGrid:disable_input(true)` for every writing mode. Darktide replaces the grid's input service with a null service in that state, so the mouse wheel and card hotspots could not deliver the click which was supposed to defocus search. | Limit grid ownership to controller navigation, retain input-legend ownership for mouse/keyboard Back handling, and reconcile ownership if the active navigation mode changes. Cursor typing therefore keeps native wheel/card input alive without adding a hook or per-frame allocation. | Source-confirmed root cause; automated cursor/controller/native-disabled ownership coverage passes, awaiting live interaction confirmation. |
| Curio `toughness & ability` ordering follow-up | `Guardian Gloriana` had Toughness as its primary line and Combat Ability Regeneration as a secondary line but appeared below equipped Toughness-only and unrelated Curios. The old packed rank made equipped state stronger than line relevance, and projection exposed only the weapon compact-perk alias provider even though Curio cards use a separate alias table (`gadget_cooldown_reduction` -> `Ability Regen`). | Make primary+secondary relevance the outer tier, then equipped state, then item level descending. Export the existing localized Curio compact labels to search at projection-build time, without adding frame work. Add a production-shaped cooldown Curio and ordering regression. | Source-confirmed dual root cause; targeted and live verification required. |
| Multi-clause progressive hierarchy | Strict AND matching collapsed every incomplete `toughness & ability & revive` Curio into one unmatched group, losing meaningful four-/three-/two-/one-line relevance and query-order intent. | Preserve strict match state separately from one packed relevance tuple: line-hit count, distinct clause coverage, primary coverage by query order, overall coverage by query order, equipped state, then item level. Store partial relevance as a negative value in the existing reusable result map so dim/promote can sort it while hide mode remains strict. Apply distinct-clause/query-order scoring to weapons and stable external-layout composition as well. | Automated twelve-level Curio, weapon, external-layout, equipped, item-level, and strict-hide coverage added; live verification required. |
| Curio hidden-description contamination | Under `health & toughness`, a `+1 Wound` Curio with only a visible Toughness secondary line appeared too high. Full trait-description prose had been copied into both the bare-text corpus and the concise primary/secondary rank fields; incidental words such as `health` could therefore create false visible-line matches. | Keep full Curio prose only for explicit `perk:` searches, outside bare text and line relevance. | Valid contamination fix, but live ordering remained wrong. This was an incomplete root-cause hypothesis, not the final fix. |
| Surviving-view UI hot-reload reconciliation | Ctrl+Shift+R recreated Better Inventory's search runtime but Darktide retained the open view, its widget, and `_better_inventory_search_widget_initialized`. The boolean therefore suppressed initialization and never delivered the already-visible query to the new runtime; old card order and alpha remained frozen. A full process restart cleared the view and masked the defect. | Replace the boolean-only gate with a module-local generation identity. Each reloaded UI module reconciles the visible query exactly once with the new runtime, while normal frames in the same generation remain idle. Release clears both fields. | Targeted regression passed. Live follow-up changed the exact-match group, proving query delivery, but partial Curio tiers still used stale ordering. UI fix is valid but incomplete alone. |
| Surviving-view comparator hot-reload reconciliation | Darktide also retained each sort-option table and Better Inventory's wrapped comparator. A new comparator manager saw the old wrapper as already configured and reused it, but that closure still called the released pre-reload search runtime. | Give every comparator-manager instance a private wrapper generation. If an unchanged Better Inventory wrapper belongs to an older generation, restore its native baseline and immediately wrap that baseline with the new manager's `search_rank`. Keep externally replaced comparators as the new native baseline and clear generation ownership on restore. | The two-generation production-path test passes, but live `health & toughness` still places a Wound-primary/Toughness-secondary Curio above stronger two-line Curios after hot reload. Keep the lifecycle correction, but mark it insufficient as the ordering fix. |
| Bounded live rank-pipeline diagnostics | Unit and production-shaped harnesses agreed while the surviving live view disagreed. Further comparator changes without observing the engine data would repeat the failed hypothesis cycle. | For exact query `health & toughness`, emit one post-scan record for each positive-rank Curio and one post-sort snapshot of the first 24 cards. Include concise primary/secondary fields, strict-match state, packed rank, selected comparator identity, and wrapper ownership. Trace runs once per module generation/query, never per frame. | Live-confirmed comparator wrapper and final grid order both followed Better Inventory ranks. Rating values were correct. Bad Wound Curio projection exposed `content/items/traits/gadget_inate_trait/trait_inate_gadget_health_segment` and `loc_inate_gadget_health` as primary-line text, falsely satisfying `health`. Diagnostic isolated projection identity leakage as final root cause. |
| Raw Curio identity isolation | Prior projection treated raw master-item paths, localization keys, and gameplay trait IDs as bare text and visible Curio line aliases. Darktide's Wound path contains `gadget_health_segment`, despite visible primary line being Wound. | Keep raw identities searchable only through explicit `perk:`/`blessing:` fields. Feed bare text and Curio line ranks only localized visible names, compact card aliases, and a bounded semantic Curio-stat alias map. Map Health, Toughness, Wound, and Stamina primary/secondary gameplay traits to current-language mod localization plus canonical aliases. | Production-shaped regression now uses exact live Wound path/key, proves `health & toughness` is not a strict Wound match, proves `wound & toughness` remains searchable, and retains Simplified Chinese semantic aliases. Targeted index/query/runtime/integration/layout tests pass; awaiting live verification. |

Current evidence:

- field input and UTF-8/native editing are alive: displayed query and character counter update;
- optimized hook set was loaded, including `InventoryWeaponsView.update` and `ItemGridViewBase._cb_on_present`;
- no search exception appears in the supplied clean log;
- no `[SearchTrace]` query event was emitted by the failed `119d0cf` candidate, so its field revision never reached the current runtime; matching and presentation were not the first failing stage;
- `be89f3d` emitted valid query/settle traces and correct match counts, but `presentation_kind=nil` proved the new runtime had missed the already-open view's pre-reload native presentation call;
- offline query/index/comparator tests are necessary but insufficient because they do not observe DMF hook timing and the live view's actual layout/widget identities;
- the latest functional screenshot proves the rich Curio relevance scan is not repeating: the correct primary-plus-secondary group remains stable after settlement;
- one million settled `SearchUI.update` plus input-routing iterations complete at about `0.4 us/frame` with zero transient allocation, while the production-gated runtime performs no idle call and releases roughly its complete 128-item projection footprint in the synthetic benchmark;
- Mod Performance Monitor's detailed `calls` column is callbacks per frame, not calls per second. Its displayed `self` value is the mean of 128 raw frame samples (about `2.13 s` at 60 FPS). The old architecture charged each full native card rebuild to BetterInventory and several searches accumulated inside that window; the new default lane performs one bounded scan/sort/in-place reorder without card construction;
- that monitor behavior does not excuse retained state: old entry identities are now removed synchronously from the reusable result map, sort priority storage is reused, and a GC-independent soak retains old layouts to prove current results remain bounded;
- Mod Performance Monitor's captured `0.134 +/- 0.119 ms` row is explicitly low confidence, so the full per-source report and its A/B benchmark are required to separate BetterInventory work from sampling variance; this caveat does not excuse avoidable recurring hooks;
- repeated Ctrl+Shift+R sessions explain why the screenshot's aggregate Lua heap cannot establish a search leak. Leak validation must use a clean process, repeated open/search/close cycles, and the release counters/cache checks rather than total heap after module reloads.
- latest bounded `[SearchRankTrace]` proves hot-reload delivery and comparator wrapping are active; final grid order exactly follows packed search ranks. Its Wound record contains correct rating `410` but false primary `health` from `trait_inate_gadget_health_segment`, so more comparator or item-level changes would be wrong.

Next investigation protocol:

1. Keep query/settlement diagnostics bounded and disabled in release builds; never log per frame or item.
2. Confirm five to twenty successive dim/promote searches no longer flicker, recreate icons, or produce a rising settled mean.
3. Capture Mod Performance Monitor's full report after at least 30 seconds in the settled Curio view so its per-hook source labels identify any remaining BetterInventory callback.
4. Run the monitor's A/B benchmark from a calm, freshly started game process; its result is authoritative when the detailed row remains low confidence.
5. Compare empty query, active settled query, and closed/reopened view without additional hot reloads. Track Lua heap deltas only across repeated view cycles in that clean process.
6. Verify optional Hide unmatched and hide-to-dim restoration separately because they intentionally retain one native presentation boundary.
7. Require live ordering/dimming plus a materially lower settled callback count and no measurable regression from the pre-search baseline before declaring the performance follow-up complete.

## State, cache, and performance rules

- State is keyed weakly by view; there is no global active view or global input widget.
- Optional query memory is a separate bounded table of plain strings keyed by character and logical view family; it contains no live view or item references.
- View cleanup is registered with `BetterInventory_view_session.lua` and repeated from `on_exit`/`destroy` fail-soft paths.
- Compile a query only after its text has remained unchanged for the 80 ms quiet interval.
- Match a compiled query without concatenating new strings per entry.
- Empty-query presentation starts a bounded cache warmer that validates at most sixteen rich item projections per frame and retains no layout snapshot. A query may use the same slices while its quiet deadline is pending; the full match pass never shares a frame with the final cold-projection slice.
- Text changes update only the bounded source string and one numeric deadline. Copy/paste, deletion, and rapid typing allocate no parser, projection, match, timer, promise, or result table until the final text settles.
- Match results use one cleared-and-reused weak-key numeric rank map; absent means unknown, zero means no relevance, positive values encode strict matches, and negative values encode partial relevance while preserving strict hide/dim state. Comparator reads use the absolute rank. Each settled scan contains only its current layout, independent of garbage-collection timing.
- Existing widgets are updated once after the settled scan. Dimming is one scalar write to `widget.alpha_multiplier`, not a clone or per-pass style rewrite.
- Controller focus blocks only the active view's main grid through its native `disable_input` contract. Mouse/keyboard focus must not disable that grid: the wheel and card hotspots need the live native input service, while the supported view input hook and transition-owned legend still prevent typing/Back from leaking into view actions. Reconcile this owned lock when navigation mode changes; never intercept every `ViewElementGrid.update`.
- Reordering or hiding is coalesced through one view-local pending timestamp. Dim mode sorts one inactive retained layout buffer and commits it through the existing grid identities; hide mode performs one native filter presentation. Continuous typing performs neither.
- Paste, clear, and dropdown changes use the same coalesced commit path.
- Cache projections by item identity plus a bounded fingerprint of relevant revision, name, rarity, ratings, traits, perks, and customization revision.
- Invalidate on item replacement, purchase, discard/sacrifice, inventory/store refresh, custom-name change, locale/master-item generation change, and mod re-enable.
- Bound query length, clause count, aliases per field, and projected text length. Proposed initial limits: 128 UTF-8 characters, 16 clauses, and 2 KiB normalized text per item.
- Do not retain full item tables in a process-lifetime cache. View caches must be weak or explicitly released.
- During the first bounded warm-up window, update projects at most sixteen records. View-local scalars keep the runtime update lane active only while warming or settling and enable filter/comparator lookup only for a settled active query. Once clear, the normal view hook, native-filter loop, and comparator do not enter search runtime code. The remaining field poll checks one bounded string revision, with no layout scan, localization, comparator rebuild, alpha scan, or allocation.
- A changed query should scan the active source once. A 200-item inventory and repeated search/clear cycle must remain bounded in the automated soak.
- Dim mode still draws unmatched cards, so it does not claim a GPU/render-count reduction. Its benefit is visual context and cheap presentation state. Hide mode reduces the final widget/draw count after the coalesced native re-presentation.

## Compatibility rules

### ItemSorting

- Native and ItemSorting filtering/sorting order is preserved. Hide mode uses native filter, Better Inventory search predicate, selected comparator, presentation. Dim mode applies the search-rank key, Better Inventory priorities, and selected comparator to the already natively eligible canonical card set, then reorders those existing identities in place.
- Hide mode passes the native filtered copy as `optional_filter_layout`. Dim mode must not call full presentation or create a second set of cards.
- Search rank is incorporated into the current Better Inventory comparator manager. Comparator wrappers remain owned by the current Better Inventory sorting domain.
- Rebinding after ItemSorting enable/disable or option changes must preserve the search key exactly once and preserve the selected comparator by display identity.

### Better Inventory custom names and colors / Name It

- The effective stored custom name is searchable.
- Searching never changes name or color ownership.
- A name edit invalidates only that item's search projection and requests one deferred refresh.

### GodRolls and God Stat Checker

- Search does not ask either mod to calculate names, grades, or full stat projections.
- Existing visible third-party colors remain presentation concerns and are not rewritten.
- Optional future grade filters require a guarded provider contract; they must not scrape decorated visible strings.

### Stuff Searcher and DumpStatFinder

- Better Inventory does not write another mod's settings or disable another mod.
- If Stuff Searcher is enabled, show one bounded compatibility warning recommending that its search UI be disabled or removed. Its obsolete layout mutation can still hide or replace entries after Better Inventory's native filter.
- If another mod already supplies a native filter function, Better Inventory calls the existing chain first and adds its own predicate with AND semantics.
- Search UI placement must not assume another mod left native scenegraph geometry untouched. Missing geometry disables only the UI for that view; filtering remains fail-soft and must never crash the view.

## Test plan

### Pure query behavior

- empty and whitespace-only query;
- case-insensitive current-locale matching with `Utf8.lower`;
- Simplified Chinese weapon/Curio names, primary and secondary perk text, blessing text, rarity labels, partial CJK terms, and UTF-8 caret length;
- exact bare localized and English rarity aliases compile as effective-rarity predicates;
- literal `%`, `.`, `+`, `-`, `*`, `?`, brackets, parentheses, `^`, and `$`;
- quoted phrases and literal `&` inside quotes;
- `&` with and without surrounding spaces;
- malformed quote, empty clause, unknown field, overlong query, and excessive clauses;
- exact number, `>=`, `<=`, `>`, `<`, and inclusive range;
- numeric tokens do not accidentally match substrings (`50` is not `500`);
- boolean fields and aliases.

### Item projection

- melee, ranged, and Curio names;
- family/pattern/mark aliases;
- weapon blessings and perks;
- Curio primary trait and perks containing percent values;
- one-, two-, three-, and duplicate four-line Curio relevance; distinct-clause coverage; primary-line preference; query-left-to-right preference; relevance before equipped state; then descending item-level ties;
- partial weapon clause coverage with the same left-to-right, equipped, and item-level tie hierarchy;
- production-shaped `gadget_cooldown_reduction` projection through the localized `Ability Regen` Curio alias, including `toughness & ability`;
- total rating versus base rating;
- each native rarity from Profane through Transcendent;
- a native Transcendent item that fails Custom Tier remains effectively Transcendent;
- a native Transcendent item accepted by active Custom Tier is effectively Sainted but remains native Transcendent;
- `sainted`/`rarity:sainted` excludes ordinary Transcendent items, while `transcendent`/`rarity:transcendent` excludes Sainted items;
- `native-rarity:transcendent` and `native-rarity:5` include both visible Transcendent and Sainted items;
- disabling Custom Tier or changing its qualifying criteria invalidates effective-rarity projections without changing native-rarity projections;
- current-locale rarity names, built-in English aliases, and a custom item named `Sainted` queried through `name:sainted`;
- custom Better Inventory/Name It names;
- missing, partial, throwing/metamethod, and version-drifted item fields;
- GodRolls decorated-name function is never called by indexing.

### View lifecycle

- opening and closing each supported view;
- switching inventory/Hadron/vendor tabs with an active query;
- store refresh and purchase;
- item replacement after crafting;
- discard and sacrifice;
- favorite/equip/new-state change;
- while mouse/keyboard text entry is active, wheel scrolling remains native and clicking/selecting a card performs the native selection action, defocuses the search field, and preserves its query;
- with session memory Off, close/reopen starts with an empty query while tab/sort/refresh changes inside the live view preserve it;
- with session memory On, close/reopen of the same view family and character restores text, then compiles and scans current data exactly once;
- remembered searches never cross character or logical view-family boundaries;
- clear removes the active and remembered query, while character change, mod disable, hot reload, and game exit clear every session record;
- empty results, clearing, and selection restoration;
- dim-to-hide and hide-to-dim changes with active and empty queries;
- one coalesced match/dim/reorder transaction after rapid typing, deletion, and paste, with no per-character scan;
- mod disable/re-enable and Ctrl+Shift+R hot reload;
- no live view state, compiled query, item reference, callback, widget, layout, result, or projection cache survives a closed view; only the optional bounded session-memory record may remain.

### Sorting and other mods

- every native inventory/store comparator;
- current ItemSorting installed and absent;
- matches precede unmatched equipped/favorite/perfect items in dim mode, then each group follows the existing hierarchy;
- hide mode sends only matches to downstream sort logic;
- empty-query order is identical to v3.1.0 for every comparator;
- equipped/favorite/perfect priorities before and after filtering;
- pre-existing native filter returning true, false, nil, or throwing;
- external spacing entries remain valid;
- custom names, MyFavorites, GodRolls, God Stat Checker, GlobalStore, and Enhanced Descriptions installed individually and in the supported combined matrix.

### Performance and memory

- cold 200-entry layouts validate at most sixteen rich projections per update and build each unchanged item once;
- rapid ten-character input performs no per-character parse/scan and at most one in-place reorder (or one hide presentation) after the quiet interval;
- each changed query performs at most one O(n) match pass and writes only changed widget alpha values;
- sort comparisons perform only O(1) cached match/priority reads and allocate nothing;
- 100 search/clear cycles do not grow view/cache counts;
- 128-card repeated dim/promote commits preserve widget identity and retain exactly two bounded layout buffers, independent of query count;
- repeated store rotations replace cache generations rather than append forever;
- fully warmed and settled idle update performs no O(n) scan or allocation;
- unrelated `ViewElementGrid` instances execute no search-owned update hook;
- settled Inventory frames do not invoke the search runtime or a separate search update hook;
- close releases query closures, layouts, widgets, and item projections;
- live QA repeats the LadyElina777-sized 130-item inventory case while monitoring Lua heap and frame latency.

## Delivery order

1. Implement and test the pure parser and matcher.
2. Implement the bounded item projection cache and custom-name/native-localization adapters.
3. Integrate inventory-only hide filtering and match-first comparator ranking through the current native seams.
4. Add the compact search field, behavior setting, focus, dimming, coalesced layout commit, empty-result handling, and cleanup lifecycle.
5. Validate native sorting and current ItemSorting together, including exact hierarchy and empty-query parity.
6. Add Armoury, GlobalStore, and Melk adapters.
7. Add Hadron modify, then the separate sacrifice adapter.
8. Run the complete verifier, 130/200-item soak, and live view matrix before packaging.

## Release gate

v3.2.0 is releasable only when clearing the query produces the same authoritative entry set and selected comparator order as Better Inventory 3.1.0 for every supported view. Search failure must fail open to the native list, never fail closed to an empty or stale snapshot.
