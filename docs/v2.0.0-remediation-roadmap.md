# BetterInventory 2.0.0 post-B20 remediation roadmap

Date: 2026-08-07
Branch: `audit/v2.0.0`
Source audit: [v2.0.0-full-project-audit.md](v2.0.0-full-project-audit.md)
B1-B20 review: [v2.0.0-b1-b20-correctness-audit.md](v2.0.0-b1-b20-correctness-audit.md)

## Purpose

This is the execution source of truth after B20. It supersedes the original Luna B21-B34 proposal after an independent commit-by-commit review found release-blocking persistence and CI defects plus unresolved manual-discard, equip, overview-refresh, and report-queue gaps.

The roadmap is ordered by risk and dependency. Safety and test trustworthiness come before profiling and large extraction. A batch should normally be one to three focused commits and must not mix an unrelated performance change with a state-machine or architecture migration.

## Current position

- B0-B20 have been implemented and recorded; “complete” in the historical ledger means the stated batch was delivered, not that every adjacent risk is closed.
- Local automated verification passes, the installed runtime matches the branch, and tracked archive parity passes.
- B22-B27 implementation and automated gates are complete; the owner has live-validated the equipment persistence hotfix.
- B28-B39 implementation is complete. B40/B41 are finalized as a release-candidate handoff; B42-B48 are complete for their stated repository slices, while B47 live evidence and optional B49 extraction remain open. B50's isolated Curio-widget repair failed owner live acceptance and is superseded by B51's native full-layout rebuild. B51-B54 pass repository verification; B51 still needs live acceptance. Diagnostics remain default-off, and no live baseline was recorded before B28.
- BI-021, BI-022, BI-023, and BI-025 are addressed by B22-B26; B27 provides the measurement foundation for BI-007/BI-008.
- BI-024 equipment persistence/reconciliation and BI-028 stale storefront synchronization remain outside batch numbering; BI-024's owner live gate passed.

## Remaining work map

| Finding | Remaining work | Priority | Batch(es) |
| --- | --- | --- | --- |
| BI-007 | Measure and eliminate remaining idle marked-grid scans | P2 | B27-B28; live baseline still required |
| BI-008 | Dirty-track panels, alignment, and pivots | P2 | B27, B29; live baseline still required |
| BI-009 | Complete live view/card/geometry teardown | P2 | B30, B34 |
| BI-011 | Bound and expose stale read diagnostics | P2 | B27, B32; domain split remains |
| BI-014 | Extract domains behind tested lifecycle/capability seams | P2 | B30-B36 |
| BI-015 | Complete declarative settings metadata | P2 | B37 |
| BI-016 | Make CI self-contained; improve case/branch coverage | P2 | B23, B39 |
| BI-017 | Reduce verifier implementation coupling | P3 | B23, B39 |
| BI-018 | Split authoring sources and generate fixed runtime bundles | P3 | B38 |
| BI-021 | Correct real DMF no-return save contract | P1 | B22 |
| BI-022 | Remove clean-checkout CI dependency on installed DMF | P1 | B23 |
| BI-023 | Retain manual discard ownership through backend settlement | P1 | B24, B31 |
| BI-024 | Live-validate local preview preservation, bounded equip retry, and authoritative reconciliation | P0 | Critical hotfixes `4e0497f`, `a4ae5ca`; B25 only if follow-up is needed |
| BI-025 | Refresh same-gear Character Overview content revisions | P2 | B26, B34 |
| BI-026 | Preserve multiple pending Curio reports | P2 | B32; live queue gate still required |
| BI-027 | Replace generic fallback semantics with typed adapters | P2 | B33; live integration matrix still required |
| BI-028 | Require confirmed storefront advancement after a rotation boundary | P0 | Critical hotfixes `a5c647b`, `8e6799d`, `d0d5803` |
| AUD-B50-01 / AUD-B51-01 | Rebuild the complete native Character Overview individual layout when Curio presence changes | P1 | B50 failed; B51 supersedes |
| AUD-B52-01 | Keep manual discard ownership through backend settlement | P1 | B52 |
| AUD-B53-01 | Fail closed when equipment retry calls throw or return invalid promises | P2 | B53 |
| AUD-B54-01 | Require every assertion to belong to exactly one named risk case | P2 | B54 |

## Critical hotfix interruption

These changes intentionally sit outside B21-B41 and do not renumber the roadmap:

- `a5c647b`, `8e6799d`, and `d0d5803` prevent a predicted reset from consuming an expired Curio storefront. A rotation-triggered scan now waits silently until every enabled character storefront proves it advanced beyond the prior boundary; schema-1 ledgers are invalidated once so an already-poisoned gate cannot hide the corrected pass.
- `4e0497f` observes Darktide's native equipment request and `a4ae5ca` prevents delayed authoritative events from overwriting an uncommitted local Y preview with X. Confirmed retries remain account/character guarded and bounded.

Resume normal batch work only after both live gates pass. Any failure remains a critical hotfix follow-up rather than being folded into an unrelated batch.

## Batch execution contract

Every batch must include:

1. A scope statement naming findings and files.
2. A failing regression/contract test before production behavior changes, whenever deterministic reproduction is possible.
3. Separate test and implementation commits when either part is non-trivial.
4. Focused tests plus the full repository verifier.
5. `git diff --check` and tracked archive parity when packaged runtime files change.
6. An explicit in-game gate recorded as `pass`, `fail`, or `not run`.
7. Updates to the full audit ledger and this roadmap.

New regressions discovered during practical validation are inserted as the smallest hotfix batch before the next dependent batch. Do not hide them in an architecture extraction.

## Ordered batches

### B21 — Validation evidence and audit reconciliation

Status: complete for the B1-B20 correctness verdicts and remediation mapping; remaining practical scenarios are explicitly recorded as not run.

Suggested commit:

- `docs: audit B1-B20 and replan post-B20 remediation`

Work:

- Record game build, mod list, resolution/UI scale, input mode, scenario, outcome, and available logs.
- Preserve `not run` where a scenario cannot be confidently exercised.
- Add every independent-review finding to the full audit.
- Insert release-blocking remediation before performance work.

Exit gate: each B1-B20 batch has a correctness verdict; all observed failures have finding IDs and assigned batches.

### B22 — Correct customization persistence contract

Status: complete in `f4efd44` and `c00e15c`; automated gate passed; in-game gate not run in this batch.

Findings: BI-021 and residual BI-012.
Dependency: none; first implementation batch.

Suggested commits:

- `test: model DMF no-return persistence contract`
- `fix: stop retrying delegated customization saves`

Work:

- Reproduce installed DMF's non-throwing `nil` return in tests.
- Treat that outcome as delegated/attempted and end BetterInventory's periodic obligation.
- Retry only unavailable or thrown invocation according to a bounded policy.
- Preserve forced lifecycle flush without claiming durable success the API cannot report.
- Remove static verifier assertions that require `save_result == true`.

Exit gate: one mutation causes one deferred call under normal DMF behavior; repeated updates do not call synchronous save forever; thrown/unavailable paths are bounded and observable.

In-game gate: rename/color/edit/delete customization, idle for at least ten seconds, leave/reopen inventory, restart game, verify persistence and no repeated BetterInventory save warnings.

### B23 — Self-contained CI and verifier foundation

Status: complete in `31b0c51`; repository-only and external-source gates passed.

Findings: BI-022 and early BI-016/BI-017 work.
Dependency: B22, so CI validates the corrected persistence contract.

Suggested commits:

- `test: separate repository and external source contracts`
- `ci: verify from a clean checkout`

Work:

- Add optional explicit DMF and Darktide source parameters.
- Run repository-owned tests without an installed game/mod tree.
- Use the provisioned Python interpreter consistently.
- Fail on zero discovered scripts and publish coverage/results artifacts.
- Add a clean-checkout workflow smoke path.
- Keep local external-source compatibility checks available and clearly labeled.

Exit gate: the exact workflow succeeds in a clean checkout; local integration mode still validates installed DMF/Darktide contracts.

### B24 — Own manual discard through native settlement

Status: complete in `f648c4c`; automated deferred-settlement gate passed; in-game gate not run in this batch.

Finding: BI-023.
Dependency: B23.

Suggested commits:

- `test: defer native manual deletion settlement`
- `fix: retain manual discard ownership through completion`

Work:

- Expose or bridge the one native delete promise without sending duplicate deletion requests.
- Keep the shared destructive token through success, rejection, and terminal cleanup.
- Define behavior if the view exits or mod disables while an irreversible request is in flight.
- Block manual, automatic, and Curio Buyer destructive work during settlement.

Exit gate: deferred-settlement tests prove no competing destructive operation starts and ownership releases exactly once on every terminal path.

In-game gate: confirm manual discard, immediately attempt another discard/automatic run/view exit, and verify one deletion/report with no lock leak.

### B25 — Equipment persistence follow-up, only if live evidence requires it

Status: conditional follow-up not required. `4e0497f`/`a4ae5ca` passed the owner's in-game validation; the focused suite covers delayed success, reopen, false/error, bounded retry, and generation invalidation.

Finding: BI-024, implemented by critical hotfixes `4e0497f` and `a4ae5ca`.
Dependency: B23; must not depend on B24 implementation details.

Conditional commits:

- `test: model delayed native equip reconciliation`
- `fix: observe equipment persistence outcomes across view generations`

Remaining work only if the critical-hotfix live gate fails or exposes missing diagnostics:

- Observe the existing native equip promise; do not issue a second backend write.
- Track slot, requested gear ID, generation, elapsed time, and true/false/error outcome with bounded diagnostics.
- Separate Darktide's three-second waiting-state timeout from actual promise completion.
- Refresh a reopened Character Overview when authoritative profile data confirms a late success.
- On failure, clear optimistic state and present authoritative equipment consistently.
- Ignore stale completion from an older character/view generation.

Exit gate: tests cover delayed success before/after reopen, false result, thrown rejection, timeout without completion, character switch, and stale generation. No path duplicates the equip request.

In-game gate: repeatedly equip then rapidly back out twice and reopen under normal and degraded network conditions; record terminal diagnostics and final server-authoritative gear.

### B26 — Refresh same-gear overview revisions

Status: complete in `f648c4c`; automated same-gear revision gate passed; in-game gate not run in this batch.

Finding: BI-025.
Dependency: critical hotfix `4e0497f` establishes the authoritative reconciliation contract; complete its live gate first.

Suggested commits:

- `test: cover same-gear Character Overview revisions`
- `fix: invalidate detailed cards on item content revision`

Work:

- Extend item identity beyond gear ID using revision/content markers with object/name fallback.
- Rebuild variable rows and reset stale colors/background/icons on revision changes.
- Preserve unchanged-frame cache behavior from B19.
- Cover native `event_item_icon_updated` and same-gear name/content updates.

Exit gate: end-to-end blueprint tests cover same gear/new object, new name, different stat-line count, icon change, and unchanged no-op.

In-game gate: customize/rename a currently equipped item, update icon-capable integrations, and verify overview details without equipping a different gear ID first.

### B27 — Hot-path measurement baseline

Status: implementation complete in `f648c4c`; 9/9 automated gate passed; in-game baseline not run.

Findings: BI-007 and BI-008.
Dependency: safety hotfixes B22-B26 complete.

Suggested commits:

- `test: define sampled hot-path counter contracts`
- `perf: add opt-in UI and async diagnostics`

Work:

- Count marked-grid scans, panel signatures/rebuilds, alignment queries/writes, pivot writes, active promises, oldest operation age, and sampled Lua memory.
- Never allocate/log every frame; counters must be opt-in and sampled.
- Capture idle, scroll, resize, tab switch, and 100 open/close baselines.

Exit gate: repeatable baseline exists and diagnostics have negligible disabled and acceptable enabled overhead.

### B28 — Dirty-generation marker synchronization

Finding: BI-007.
Dependency: B27 baseline.

Status: Complete in `8f33b3d`; automated marker regression passes. In-game gate not run.

Suggested commits:

- `test: cover marker invalidation generations`
- `perf: replace idle marked-grid scans with dirty synchronization`

Work:

- Refresh marked state on inventory, favorite, view, integration, or layout generation changes.
- Preserve a conservative sampled fallback when an integration exposes no generation.
- Keep weak-key lifecycle ownership and disable cleanup.

Exit gate: idle views stop scanning; mark/unmark, item replacement, rebind, and integration absence still update correctly.

In-game gate: scroll, mark/unmark, change layout, reopen, and combine MyFavorites with Equipped Icon+.

### B29 — Dirty panel, alignment, and pivot updates

Finding: BI-008.
Dependency: B27 baseline.

Status: Complete in `8569ca9`; geometry/integration regression matrix passes. In-game gate not run.

Suggested commits:

- `test: cover panel and geometry invalidation`
- `perf: gate composition writes by state generation`

Work:

- Cache only stable inputs and invalidate on data, settings, viewport, scenegraph, or integration generation.
- Avoid unconditional pivot setters and repeated force/query calls.
- Preserve resize and native single-column fallback behavior.

Exit gate: idle frames perform zero unnecessary rebuild/alignment/pivot writes while each relevant change updates exactly once.

In-game gate: 1080p/1440p/4K, UI-scale extremes, 2-5 columns, native mode, controller focus, and vendor panels.

### B30 — Scoped ViewSession teardown

Finding: BI-009.
Dependency: B26 and B29 define content/geometry invalidation.

Status: Complete for the scoped lifecycle seam in `7f80070`; full card/geometry restoration remains B34. In-game gate not run.

Suggested commits:

- `test: table-drive view teardown ownership`
- `refactor: introduce scoped ViewSession restoration`

Work:

- Snapshot only values BetterInventory mutates.
- Restore controls, legends, card blueprint references, geometry, focus, wrappers, and panels exactly once.
- Make teardown idempotent for view exit, external close, disable, hot reload, and character switch.

Exit gate: repeated teardown/rebind leaves native state and third-party changes intact.

In-game gate: disable/re-enable while Character Overview, inventory, vendor, Armoury, and GlobalStore are open.

### B31 — Destructive-operation arbiter and discard extraction

Findings: BI-003 residual architecture, BI-014, and BI-023 follow-up.
Dependency: B24 supplies correct manual settlement semantics.

Status: Complete for generation-owned destructive-operation arbitration in `65a7276`; discard domain/UI extraction remains B35. In-game gate not run.

Suggested commits:

- `test: table-drive destructive terminal transitions`
- `refactor: extract operation arbiter and discard domains`

Work:

- Replace scattered flags with explicit operation states and generation-owned terminal cleanup.
- Extract manual selection/confirmation, automatic scheduling/revalidation, and backend settlement from the features god module.
- Keep irreversible operations owned even when UI scheduling is cancelled.

Exit gate: every terminal path releases once; operation overlap rules are explicit; behavior remains identical to B24.

### B32 — Curio operation context and report queue

Findings: BI-011, BI-014, and BI-026.
Dependency: B31 operation ownership vocabulary.

Status: Complete for the bounded account-scoped report queue in `cf8fba2`; scheduler/context extraction remains B36. In-game gate not run.

Suggested commits:

- `test: preserve multiple account-scoped Curio reports`
- `refactor: split Curio operation, ledger, and reporting state`

Work:

- Snapshot account, character, menu, rotation, and generation at operation start.
- Distinguish cancellable reads from irreversible purchase POSTs.
- Replace the one-slot report with a bounded, deduplicated account queue.
- Define timeout/unknown diagnostics without retrying ambiguous purchases.

Exit gate: two reports survive before Morningstar, deliver in order once, remain account-isolated, and stale continuations cannot mutate a new context.

In-game gate: Morningstar/Operative Selection, two rotations before report delivery, character switch at every stage, insufficient funds, timeout, and GlobalStore concurrency.

### B33 — Typed integration and fallback adapters

Findings: BI-014 and BI-027.
Dependency: B23 verifier source-contract split.

Status: Complete in `4051b7c`; typed lookup/invocation and conservative registry fallback tests pass. In-game integration matrix not run.

Suggested commits:

- `test: define capability-specific fallback semantics`
- `refactor: extract guarded integration adapters`

Work:

- Replace generic false-returning module fallback with typed per-domain fallbacks.
- Put method lookup and invocation inside protected boundaries.
- Keep core hooks unaware of optional-mod private layouts.
- Make settings-registry failure conservatively refresh dependencies.

Exit gate: absent, partial, throwing/metamethod, and version-drifted integrations fail closed without suppressing required core refreshes.

In-game gate: each supported integration alone and documented high-risk combinations.

### B34 — Character Overview view-model extraction

Findings: BI-009, BI-014, and BI-025 follow-up.
Dependency: B26 and B30.

Status: Complete in `6fc1b6c`; pure identity/revision/model/reset seams are covered by Character Overview fixtures. In-game card teardown and rapid equip validation remain B40 evidence gates.

Suggested commits:

- `test: add pure equipped-card view-model fixtures`
- `refactor: separate overview content, geometry, and coordination`

Work:

- Convert weapon/curio input into a complete rebuilt view model.
- Separate content derivation, blueprint construction, geometry, and native coordination.
- Ensure variable children cannot survive from the previous item.

Exit gate: fixtures prove item identity, background, rarity, stats, icons, selection, and empty state never leak between items.

### B35 — Split feature domains

Finding: BI-014.
Dependency: B31 and B33.

Status: Complete in `c6f2923`; marker, sorting, panel, and integration policy boundaries are explicit and covered. The large feature module remains the composition root by design; deeper mechanical relocation is deferred until live behavior evidence supports it.

Suggested commits:

- `refactor: extract sorting and marker lifecycle`
- `refactor: extract panel and navigation composition`

Work:

- Reduce `BetterInventory_features.lua` by moving sorting/markers, panels/navigation, and integration coordination behind explicit APIs.
- Preserve ownership registries and hook installation in one composition root.
- Avoid a mechanical line-count split with shared hidden globals.

Exit gate: each extracted domain has isolated fixtures, no cyclic imports, and no behavior change in full verification/in-game gates.

### B36 — Split Curio acquisition domains

Finding: BI-014.
Dependency: B32.

Status: Complete in `9eae0e7`; context snapshot, bounded report queue, and retry policy domains are wired and covered. Backend scheduler/read/purchase orchestration remains centralized to avoid duplicating irreversible paths.

Suggested commits:

- `refactor: extract Curio scheduler and backend operations`
- `refactor: extract Curio ledger and reporting`

Work:

- Separate scheduling/context, discovery/read operations, purchase/revalidation, rotation ledger, and reports.
- Keep one explicit orchestrator and immutable operation snapshots.

Exit gate: modules have narrow dependencies; account/generation tests remain deterministic; no purchase path is duplicated.

### B37 — Complete declarative settings metadata

Finding: BI-015.
Dependency: B33 typed registry fallback.

Status: Complete in `794057d`; all 240 active settings, including generated character slots, expose deterministic owner/visibility/migration/title/test metadata and an audited manifest digest.

Suggested commits:

- `test: cover settings ownership and migrations`
- `refactor: make registry metadata authoritative`

Work:

- Add owner, visibility predicate, migration key, title, and test metadata.
- Detect duplicate IDs, orphan metadata, and unregistered active settings.
- Preserve generated character-slot settings and upgrade values.

Exit gate: deterministic manifest and no duplicated manual dependency/title wiring.

### B38 — Generate data/localization runtime bundles

Finding: BI-018.
Dependency: B37 and confirmed DMF descriptor compatibility.

Status: Complete for the safe scope in `69df396`; deterministic source/hash manifest and archive checks are active. Authoring-file splitting remains explicitly deferred because fixed DMF descriptor entry points and clean-install compatibility have not been proven safe.

Suggested commits:

- `test: lock generated runtime references`
- `build: generate deterministic settings and localization bundles`

Work:

- Split authoring sources by feature/locale while preserving fixed DMF entry points.
- Generate packaged runtime files deterministically and validate archive content.
- Document deferral if DMF constraints make the split unsafe.

Exit gate: schema/localization drift, clean install, archive parity, and deterministic regeneration pass.

### B39 — Granular tests, branch gates, and verifier cleanup

Findings: BI-016 and BI-017.
Dependency: extracted domains B31-B38 provide testable seams.

Status: Complete in `1e749bc`; 43 named risk cases, zero-test/manifest drift rejection, risk-weighted runtime coverage gates, and AST-discovered local-module checks are active. Branch coverage and live-engine evidence remain B40/B41 work.

Suggested commits:

- `test: report case-level results and destructive branch gates`
- `test: replace remaining implementation policies`
- `ci: enforce risk-weighted coverage thresholds`

Work:

- Split giant scripts or register named cases so one assertion does not hide the remaining suite.
- Add branch/outcome matrices for destructive and async terminal paths.
- Exclude or separately report declarative files so aggregate coverage is not misleading.
- Prefer behavior/AST checks; retain regex only for literal external/package contracts.

Exit gate: CI reports case-level failures, zero-test discovery fails, and thresholds emphasize main/lifecycle/destructive code rather than localization volume.

### B40 — Live-engine stress and compatibility evidence

Dependency: applicable B22-B39 batches.

Status: Finalized for owner execution in [`docs/b40-live-validation-record.md`](b40-live-validation-record.md). Repository/package gates pass, but all live rows are currently `Not run`; B40 is not a live pass yet.

Suggested commits:

- `docs: record repeatable stress and compatibility evidence`

Work:

- Run 100 open/close cycles, ten-minute scrolling, rapid equip/exit loops, repeated tab/layout changes, controller paths, and integration combinations.
- Record build, mod list, resolution, UI scale, input, memory checkpoints, diagnostics, and warnings.
- Compare with B27 baseline and investigate unexplained growth or stale state.

Exit gate: no unexplained BetterInventory crash, stale UI, destructive overlap, resource warning, or post-GC growth trend.

### B41 — 2.0.0 release closure

Dependency: all release-required batches and in-game gates.

Status: Repository closure finalized in [`docs/v2.0.0-release-fingerprint.json`](v2.0.0-release-fingerprint.json). The tracked archive, manifests, verifier, and audit dispositions are recorded. Stable-release closure remains externally blocked until B40 is completed and accepted.

Suggested commits:

- `docs: close 2.0.0 audit dispositions`
- `build: verify final 2.0.0 archive`
- `chore: record release validation fingerprint`

Work:

- Mark every finding complete, explicitly deferred with rationale, or externally blocked.
- Update changelog, README, user/architecture docs, and release notes.
- Rebuild the tracked ZIP from the exact tree and record SHA-256.

Exit gate: self-contained CI and full local integration verifier green; archive parity green; required live gates passed; clean worktree; no unassigned open P1/P2 finding.

## Immediate next move

Run the matrix in [`docs/b40-live-validation-record.md`](b40-live-validation-record.md) with the packaged/deployed 2.0.0 runtime. Mark every row `Pass`, `Fail`, or an explicitly accepted external limitation, then update the release fingerprint and audit ledger. Keep `debug_enable_hot_path_diagnostics` disabled during ordinary play; enable it only for a controlled baseline if performance evidence is needed.

## Post-B41 remediation sequence

The secondary B22-B41 audit identified work that can be completed in the
repository before the final live gate. These batches are intentionally narrow:

### B42 — Production integration fixtures and optional-boundary regressions

Finding: `AUD-B34/36-INT`, `AUD-B40-01`. Load the real extracted Character
Overview, feature-domain, and Curio-domain modules through production-like test
harnesses instead of replacing them with fake return tables. Add strict-table
fixtures for optional scenegraph and native capability boundaries.

Exit gate: real module wiring is exercised, missing optional fields fail closed,
and the full verifier remains green.

Status: Complete in `b29978c` for repository scope. The production-facing
settings, feature, and Curio harnesses now execute the real extracted modules;
live crash and compatibility acceptance remains B47.

### B43 — Immutable Curio operation snapshots and coherent rotation boundaries

Findings: `AUD-B36-01`, `AUD-CURIO-ROTATION-01`. Capture one immutable snapshot
per scan/purchase/report operation containing account, context, entry, token,
and read generation. Require all storefronts participating in a pass to expose
one coherent post-refresh boundary; mixed or missing boundaries remain pending
and retry without evaluating or purchasing offers.

Exit gate: deterministic late-callback/account-switch and mixed-boundary tests
pass; no purchase path is duplicated. Backend timing remains a B47 live gate.

Status: Complete in `06315f8` for repository scope. Scan continuations now use
an account/context/entry/read-generation snapshot, and a pass waits when
storefront boundaries are mixed or missing after synchronization is expected.

### B44 — Authoritative settings metadata contract

Finding: `AUD-B37-01`. Separate active setting registration from explicit
metadata declaration, detect missing/extra metadata, expose executable
visibility predicates, and validate title/tooltip IDs against localization.

Exit gate: the generated metadata digest, localization contract, migration
coverage, duplicate detection, and negative audit cases all pass.

Status: Complete in `cfc2ff2` for repository scope. Settings metadata audits
now report missing type/title/tooltip contracts, visibility is executable,
localization IDs are checked against the loaded table, and negative synthetic
registrations fail as intended.

### B45 — Assertion-level and branch-aware verification gates

Finding: `AUD-B39-01`. Give each named case an executable assertion boundary,
add branch/outcome matrices for destructive and asynchronous terminal paths,
and reject unassigned non-declarative runtime modules instead of assigning them
zero coverage.

Exit gate: a deliberately failing assertion identifies only its case, branch
gates are machine-readable, and unassigned behavior-bearing modules fail the
verifier.

Status: Complete in `324ae1e` for repository scope. The manifest validates
source ranges, failed tracebacks identify one case while later cases remain
`not_run`, twelve high-risk async/destructive outcomes are matrix-gated, and
coverage rejects unassigned non-declarative modules. Current 72.49% aggregate
line coverage and per-module floors pass; engine branch evidence remains B47.

### B46 — Safe mechanical architecture extraction

Findings: `AUD-B35-01`, `AUD-B34-01`. Move complete policy/state clusters behind
narrow modules without duplicating ownership or changing behavior. Make the
Character Overview model either the actual blueprint source of truth or remove
its unused attachment. Do not combine this with new behavior.

Exit gate: no cyclic imports, no duplicate hook ownership, giant-module size is
reduced by real responsibility transfer, and all existing tests plus static
contracts pass.

Status: Complete in `c883ab3` for the safe first slice. Composition invalidation
and scenegraph input tracking now live in
`BetterInventory_feature_composition.lua`, load through the production-facing
feature fixture, and preserve strict optional-field behavior. The unused
Character Overview config-model attachment was removed. The remaining sorting,
panel, and integration state machines are intentionally deferred to B48/B49.

### B47 — Final live acceptance and release decision

Dependency: B42-B46 and `665801b`. Rerun B40-01/B40-02/B40-06 first, then the
complete live matrix. Record every result, diagnostics sample, optional-mod
combination, and crash/log evidence. Stable 2.0.0 remains blocked until all
required rows pass or an explicit external limitation is accepted.

Status: Pending owner evidence. Repository gates are ready, but this batch
cannot be completed from source-only execution because it requires the live
Darktide engine, real backend timing, crash logs, and controlled memory samples.

### B48 — First staged ItemSorting integration extraction

Finding: `AUD-B35-01`. Move ItemSorting integration state and native-option
assembly behind one adapter while preserving the existing feature API.

Exit gate: the adapter is production-loaded, no duplicate hook registration or
cyclic local-module load is introduced, the slice has a named coverage policy,
and the full verifier remains green.

Status: Complete in `6bbfd37`; B49 continues the remaining panel, optional
integration, and discard-state slices.

### B49 — Remaining feature state-machine extraction

Finding: `AUD-B35-01`. Continue B48 in small reversible slices: panel
composition, optional integrations, then discard state. Each slice must move
complete functions, add a production fixture, preserve one hook owner, and keep
package/archive parity.

Exit gate: no duplicate hook registration or cyclic local-module load, each
extracted slice has named coverage/outcome gates, and the full verifier remains
green before the next slice starts.

### B50 — Empty Curio widget lifecycle repair

Finding: `AUD-B50-01`. This is a focused correctness hotfix discovered during
owner validation, and may predate B42-B47. When Character Overview creates an
empty Curio slot, Darktide keeps that widget's fixed native placeholder
blueprint even after its `gadget_item_slot.update` callback fills
`content.item`. The result is a basic/native-looking Curio card until the
overview is reopened. Custom names/colors are not required to trigger it.

Status: Superseded. The B50 fixture passed, but owner live acceptance reproduced
the blank/default card. B51 replaces the incomplete one-widget strategy.

Work delivered:

- Reconcile only Character Overview Curio widgets after native update.
- Re-enter existing widget creation hook with a native gadget config when item
  presence changes in either direction.
- Preserve callback names, scenegraph ID, placement, index, native icon
  ownership, and old-blueprint cleanup.
- Cover plain level-420 empty→equipped and equipped→empty transitions in the
  production settings harness.
- Require the reconciliation seam through `tests/verify.ps1`.

Exit gate: focused test, Lua syntax/AST checks, full verifier, source/live
runtime parity, archive parity, and package rebuild pass. In-game gate: equip a
vanilla Curio from each empty slot and immediately return to Character Overview;
repeat with custom name/color, multiple secondary lines, unequip, locked slot,
and rapid close/reopen. Card format, background, icon, item level, and every
Curio line must be current on the first frame without reopening.

### B51 — Native full-layout Curio presence rebuild

Findings: `AUD-B50-01`, `AUD-B51-01`. Retain the Curio presence/type mismatch
detector, but delegate reconstruction to Character Overview's native
`_switch_active_layout` path. Do not manually clone or replace one widget.

Status: Complete in `a52d6a8` for repository scope. Both transition directions
pass deterministic coverage and the full verifier. Live acceptance remains
required using the B50 in-game gate above.

### B52 — Manual discard settlement ownership

Finding: `AUD-B52-01`. Popup disappearance after manual confirmation must not
release destructive-operation ownership while native deletion is pending.

Status: Complete in `b4d815c`. The deterministic flow covers confirm, popup
removal, reconciliation, competing-operation rejection, and terminal release.

### B53 — Equipment retry invocation safety

Finding: `AUD-B53-01`. Build persistence retries atomically: each required Items
call must be protected and return a compatible promise. Any failure rejects the
whole attempt into the existing bounded retry policy.

Status: Complete in `a9eac7d`. Synchronous throws, nil/invalid promises, no
starting-snapshot advancement, and later recovery are covered.

### B54 — Assertion-to-risk-case integrity

Findings: `AUD-B39-01`, `AUD-B54-01`. Parse each Python test and require every
`assert` statement to fall in exactly one case-manifest range.

Status: Complete in `b1df011`. All 14 tests, 45 named cases, and 12 critical
async/destructive outcomes pass; unmapped or overlapping assertions fail test
discovery.

## Completion record format

Append each future batch to the full audit ledger:

| Batch | Commits | Findings | Automated gate | In-game gate | Status |
| --- | --- | --- | --- | --- | --- |
| Bxx | `hash` / pending | BI-xxx | result | pass/fail/not run + evidence | planned/in progress/complete |
